name: "Check for newer image versions"

on:
  workflow_dispatch:
  schedule:
    # At midnight UTC every day
    - cron: "0 0 * * *"

jobs:
  digest-update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    steps:
      - uses: actions/checkout@v7.0.0
        with:
          persist-credentials: false
      - uses: navikt/digestabot@v1.2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          team: meldekort
